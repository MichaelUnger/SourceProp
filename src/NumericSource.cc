#include "NumericSource.h"

namespace prop {
  double
  NumericSource::LambdaEsc(const double E, const double A)
    const
  {
    return 0;
  }

  double
  NumericSource::LambdaInt(const double E, const double A)
    const
  {
    return 0;
  }

}
